{{/*
clusterroles.yaml — ClusterRole for each enabled operator.
*/}}

{{- if .Values.operators.kubernetes.enabled }}
---
# ClusterRole for lab-kubernetes
# Needs broad access: core lab.inklogic.tech CRDs, shadow CRDs, Fleet, Rancher, core K8s resources.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: lab-kubernetes
  labels:
    {{- include "lab-operators.labels" . | nindent 4 }}
    app.kubernetes.io/name: lab-kubernetes
rules:
  # Core Lab CRDs — watch + finalizer management
  - apiGroups: ["lab.inklogic.tech"]
    resources:
      - organizations
      - organizationprimers
      - accounts
      - teams
      - members
      - products
      - firefighters
      - clouds
      - environments
      - zones
      - deploymenttargets
    verbs: ["get", "list", "watch", "update", "patch"]

  # OrganizationPrimer delete — cleanup/delete path
  - apiGroups: ["lab.inklogic.tech"]
    resources: ["organizationprimers"]
    verbs: ["delete"]

  # Shadow CRDs owned by the Kubernetes operator
  - apiGroups: ["kubernetes.lab.inklogic.tech"]
    resources: ["*"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # Tech provider CRDs watched by this operator
  - apiGroups: ["kubernetes.lab.inklogic.tech", "postgres.lab.inklogic.tech"]
    resources: ["clusters", "postgres"]
    verbs: ["get", "list", "watch"]

  # Application CRDs — watched by ApplicationController
  - apiGroups: ["product.lab.inklogic.tech"]
    resources: ["applications"]
    verbs: ["get", "list", "watch"]

  # Database CRDs — watched by DatabaseController
  - apiGroups: ["postgres.lab.inklogic.tech"]
    resources: ["databases"]
    verbs: ["get", "list", "watch"]

  # Fleet resources
  - apiGroups: ["fleet.cattle.io"]
    resources:
      - bundles
      - gitrepos
      - bundlenamespacemappings
      - clusters
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # Rancher management resources
  - apiGroups: ["management.cattle.io"]
    resources:
      - projects
      - projectroletemplatebindings
      - roletemplates
    verbs: ["get", "list", "watch", "create", "update", "patch"]

  # Rancher cluster registration tokens — read by KubernetesClusterController for cluster import
  - apiGroups: ["management.cattle.io"]
    resources: ["clusterregistrationtokens"]
    verbs: ["get", "list"]

  # Rancher provisioning clusters — managed by KubernetesClusterController
  - apiGroups: ["provisioning.cattle.io"]
    resources: ["clusters"]
    verbs: ["get", "list", "create", "update", "delete"]

  # Core Kubernetes resources
  - apiGroups: [""]
    resources: ["namespaces", "configmaps"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]

  # Secrets — create/update/patch/delete for credential management and cleanup
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # ServiceAccounts — managed by ApplicationController for workload identity (delete on app removal)
  - apiGroups: [""]
    resources: ["serviceaccounts"]
    verbs: ["get", "create", "update", "delete"]

  # Deployments — managed by ApplicationController for workload reconciliation (delete on app removal)
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "create", "update", "delete"]

  # HorizontalPodAutoscalers — managed by ApplicationController for scaling (delete on app removal)
  - apiGroups: ["autoscaling"]
    resources: ["horizontalpodautoscalers"]
    verbs: ["get", "create", "update", "delete"]

  # Kuma mesh resources — managed by ApplicationController for service mesh registration (delete on app removal)
  - apiGroups: ["kuma.io"]
    resources: ["meshservices", "meshhttproutes"]
    verbs: ["get", "create", "update", "delete"]

  # Events
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
{{- end }}

{{- if .Values.operators.github.enabled }}
---
# ClusterRole for lab-github
# Needs: core lab.inklogic.tech CRDs (watch + finalizer), github.lab.inklogic.tech (full), secrets.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: lab-github
  labels:
    {{- include "lab-operators.labels" . | nindent 4 }}
    app.kubernetes.io/name: lab-github
rules:
  # Core Lab CRDs — watch + finalizer management
  - apiGroups: ["lab.inklogic.tech"]
    resources:
      - organizations
      - accounts
      - teams
      - members
      - products
      - firefighters
    verbs: ["get", "list", "watch", "update", "patch"]

  # Github provider CRD — full access (owned by this operator)
  - apiGroups: ["github.lab.inklogic.tech"]
    resources: ["*"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # Secrets — reading GitHub credentials and provider config
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch"]

  # Events
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
{{- end }}

{{- if .Values.operators.discord.enabled }}
---
# ClusterRole for lab-discord
# Needs: core lab.inklogic.tech CRDs (watch + finalizer), discord.lab.inklogic.tech (full), secrets.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: lab-discord
  labels:
    {{- include "lab-operators.labels" . | nindent 4 }}
    app.kubernetes.io/name: lab-discord
rules:
  # Core Lab CRDs — watch + finalizer management
  - apiGroups: ["lab.inklogic.tech"]
    resources:
      - organizations
      - accounts
      - teams
      - members
      - firefighters
    verbs: ["get", "list", "watch", "update", "patch"]

  # Discord provider CRD and shadow CRDs — full access
  - apiGroups: ["discord.lab.inklogic.tech"]
    resources: ["*"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # Secrets — reading Discord bot token and provider config
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch"]

  # Events
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
{{- end }}

{{- if .Values.operators.postgres.enabled }}
---
# ClusterRole for lab-postgres
# Needs: core Lab CRDs (watch), postgres.lab.inklogic.tech (full), secrets, batch/jobs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: lab-postgres
  labels:
    {{- include "lab-operators.labels" . | nindent 4 }}
    app.kubernetes.io/name: lab-postgres
rules:
  # Core Lab CRDs — read org membership and resource specs
  - apiGroups: ["lab.inklogic.tech"]
    resources:
      - organizations
      - teams
      - members
      - accounts
      - firefighters
    verbs: ["get", "list", "watch", "update", "patch"]

  # Postgres provider and tech CRDs — full ownership
  - apiGroups: ["postgres.lab.inklogic.tech"]
    resources: ["*"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # S3 ObjectStore CRD
  - apiGroups: ["s3.lab.inklogic.tech"]
    resources: ["objectstores"]
    verbs: ["get", "list", "watch"]

  # Kubernetes Jobs — create backup/restore jobs
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

  # Secrets — reading DB credentials and provider config
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]

  # ConfigMaps — operator configuration
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "watch"]

  # Events
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
{{- end }}
